---
"$extends":
  - base/normal.json
when:
  source:
    - ${COMMANDUNIT_HOSTFSROOT_MOUNTPOINT}/${COMMANDUNIT_PWD}/src/main/scripts/lib/core.sh
  cmd: set
  args: []
then:
  exitCode:
    - EQUAL
    - 0
  stdout:
    present:
      - REGEX:.*
  stderr:
    absent:
      - REGEX:.+

